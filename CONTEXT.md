# studyhelper

一个"过手总结台"：把一节课整理成「量 — 公式 — 关系」的树，边写边连。两个界面共用一套数据：
**白板**（拿笔就能画，卡片 + 笔迹 + 关系）和**笔记**（纯文本 + 引用）。这份文件只装这个项目
自己的词汇 —— 通用编程概念不进来。

## 语言

### 板与内容

**板（白板）**：
一块可以随便画、随便摆卡片的纸面。文件是 `data/board-*.md`（内容其实是 JSON，但仍是 `.md`）。
_避免_：画布、canvas（那说的是实现里的两层位图）

**板文件**：
`data/` 里那些 `.md` 在应用这一侧的统一说法（板 + 笔记都算）。"打开哪一个、新建叫什么名字、
怎么读写"这一族规矩只在 `files.js` 一处。
_避免_：文档、文件管理

**路径（相对路径）**：
`data/` 里一个文件或一层的身份：`大物/电磁学/board-第一章.md`。
**根上的老文件就是它的名字**（里面没有 `/`），前言里不用区分这两者。
_避免_：文件名（那只是它的最后一段）、绝对路径（一律不合法 —— 不许跑出 `data/`）
规矩只在 `paths.js` 一处，服务端和前端共用同一份。

**分层 / 一层**：
`data/` 里的**普通子目录**（`data/大物/电磁学/`）。它只改变东西**放在哪儿**，
不改"什么算一张板"。口头叫"层"，磁盘上就是目录 —— 所以你在资源管理器、Git、VSCode 里
看到的结构，和数据里的结构是**同一个**（这也是为什么没有做成"文件名里塞层前缀"）。
_避免_：分类、标签、目录树（树说的是左栏把它摆出来的那个样子）

**在这一层建 / 在根上建**：
"新东西放进哪儿"这件事，**在框里看得见、点得到**（2026-09-21 重做）：
「建到哪一层」那一排是**按钮**（最左边永远是「根目录」，初值 = 你点的那一行 /
你现在所在的那一层），名字框里**只有名字**。想一次往下开几层的熟手照样可以在名字里打
`电磁学/第一章`（它和选中的那一层**叠加**：点的是起点、打的是往下走几格）。
入口：目录行上的「＋白板」/「＋分层」（位置 = 你点的那一行）、顶部「＋ 白板」/「＋ 分层」
（初值 = 你现在所在的那一层）。路径前缀是应用替你拼的（`boardPath` 一处），
所以你在框里**只打名字**。和它相对的是「移动」那条路 —— 那条要写完整路径。
_避免_：新建路径、当前目录（那是 shell 的说法，这里没有"当前目录"这个概念）
⚠ 前缀只有一处说了算：自己**别再加 `board-`**（README 第 47 条旁边那条 selfcheck 的坑）。
⚠ 同理，**"哪一层"也只拼一次** —— `Ask` 交出来的已经是"层 + 名字"，
  调用方别再 `joinPath(where, …)`（2026-09-21 就是这么把路径套了两遍的，见 README 第 54 条）。

**改名**：
只换一个东西**最后那一段**（`board-第一章.md` → `board-第二章.md`）。所在的那一层**不动** ——
换层是「移动」的事，"名字归名字、位置归位置"是这两件事的分界线。
左栏那一行上，**整行随便点都打开**（名字那一格也算），改名走**行尾那颗「改名」按钮**。
_避免_：重命名、换一层（那是移动）、移到哪儿（那是移动那个框）
规矩：`paths.js` 的 `isRenamed` 只看最后一段；落盘走服务端 `/api/move` 那一个入口。
★ 为什么不是"点名字那一格 = 改名"（2026-09-17 曾经是）：名字那一格是 `flex: 1`，
占一行里绝大部分宽度 —— 想打开一张板时手指就落在那里，于是**经常开成改名框**
（用户 2026-09-21 报的「点白板的时候经常点到重命名很难受就打不开白板」）。
打开是每秒都在做的事，它必须**一次点击就成**；改名是特意去做的事，给它一颗看得见的按钮
（划过这一行就在）不算负担。

**询问框**：
应用**自己画的**那个问话弹窗（新建白板 / 新建一层 / 移到哪儿 / 丢掉改动…）。
从前用的是浏览器的 `prompt` / `confirm`，换掉的理由是它**把整棵树盖住** ——
而这里问的偏偏就是"放进哪一层"。表在 `App.jsx` 的 `ASK` **数据**里，长什么样在 `styles.css`。
★ **它里面能放"控件"**，不只是一段说明 + 一个输入框：「建到哪一层」那一排就是
  一排**按钮**（`.ask-layers` / `.ask-layer`），点一下就换一层，不用把父层名字打一遍。
_避免_：弹窗、对话框、modal、dialog（`dialog` 是实现里的那个标签，说话时叫"询问框"）
⚠ 它是 `<dialog open>`（**非模态**）—— Esc **不会**自动关闭（那是 `showModal()` 才有的行为），
  得自己接：`Ask` 里的 `onKeyDown` 就是干这个的（2026-09-21 修，见 README 第 54 条）。

**卡片**：
板上一块带一圈边、能拖能缩的内容块，分**公式卡**和文字卡（便签）。
两个"它自己长什么样"的字段（都只在不是默认值时写盘）：`scale`（倍率，字号/内边距/圆角/宽高一起乘）
和 `rot`（旋转弧度，绕卡片自己的中心）。
_避免_：节点、元素、dom

**按内容量尺寸**：
卡片宽高不按字数估、也不按你圈的那块笔迹，而是**按渲染出来的内容**量一次（"框贴合公式和字"）。
两条轴都量：宽量内容的自然宽度、高量内容高度 + 内边距。规矩只在 `card-fit.js` 一处。
_避免_：自适应、自动布局（那是排版层的说法）

**笔迹 / 一笔**：
你画下的一笔（按下到抬起）。是板上最原始的东西，不承载任何语义。
_避免_：线条、stroke（实现里叫 stroke，说话时叫"一笔"）

**墨迹块**：
挨得够近的一撮笔迹被当成**一个整体**来看待（用来认"写在这条连接中点旁边的那几个字"）。它不存盘，每次现算。
_避免_：字迹块、笔画组

**板框**：
你框选之后亲手留下的一个整体：里面的笔迹 / 卡片从**那一刻**起属于它（以后不会自己再并进来），
框线按这些成员的包围盒现算，可以整体拖动、写一个标题。它是**宣告**出来的，不是应用猜的。
_避免_：固定块、分组、group、区域、区块（`frames` 是它的存储字段名）

### 关系

**连接**：
你**宣告**的一条关系：用「→ 箭头」工具划一笔，两端落在两张卡片或者两个板框上。
还有一种**你画出来**的连接：一笔线，两头各落在一张卡里（免费路，不用宣告）。
两者都是板上"你明确说过"的关系 —— **连接 ≠ 推断**（推断那一族见下条）。
_避免_：连线、边、edge、线（"线"说的是笔迹）

**端点**：
板上**能当一条连接两端**的那个东西 —— 只有**卡片**和**板框**两种（墨迹不是，
ADR-0001 之后连"没成卡的字迹"也不算）。"这一点落在谁身上"也归它管：
进框优先、**卡片优先于板框**，都不在框里就看离边最近（三个半径各有各的用处：
免费路宽容 8、箭头吸附 40、条件搜索 64）。规矩只在 `nodes.js` 一处。
_避免_：节点、元素、目标（`node` 是实现里的字段名）

**端点清单**：
把"板上有哪些端点、每个板框多大"**一次算好**的那份结果（`nodeList(board)` 的返回值）。
它是**性能上的一个参数**：`nodeAt` / `nodeById` 不收它就自己算一份，
而"算一份"里包含给每个板框扫一遍成员求包围盒 —— 在"每一笔都要问两次"的循环里，
代价会变成 O(笔数 × 框成员数)（README 第 47 条：650ms/帧）。
所以**热循环里必须算一次传进去**。它不是缓存、也不存盘，就是一次计算的搬运。
_避免_：索块、缓存、索引（索引是另一回事 —— 那是 `createInkIndex`）

**关系**：
应用从**位置**读出来的从属（包含 / 重叠 / 挨着 / 孤岛）。你没说过，是推断 —— 所以面板里
单独一节标注"按位置读出来的"。**关系 ≠ 连接**：前者是读的，后者是你宣告的。

**条件**：
写在那条连接**中点旁边**的几个字（或放在那儿的一张卡）—— 位置说了算，不用你声明。
位置会读错、也会读不到，所以配了两个手动口子：**「这个条件不算」**（面板那一行的 ✕）
和**「条件就是它」**（那一行的 ∈ → 点一下目标）。都存在 `cond` 上
（你画的连接存在那一笔上，你连的存在那条记录上）。
_避免_：前提、备注（那是"内容"的说法，这里是"这条关系成立的条件"）

**条件不算 / 条件就是它**：
你对条件的两种说法：否决（"那撮字不是它的"）和指定（"就是这张卡 / 这撮字"）。和连接上那排词是同一族的口子：**优先于位置**、带一条回头路（↺ 回到按位置读）、
只在你说过时才写进文件。一个字段（`cond`）装这两个说法，所以它们不会互相打架。

**推导链**：
由「推导」那几条连接串起来的 A → B → C，面板会标出**哪一步缺条件**。

### 看这块板的视角

**纸面**：
板的背景（纯白 / 方格 / 横线 / 点阵）。它是"我怎么看这张板"，不是板的内容，所以不进板文件。
_避免_：背景图、底纹（底纹是实现里的画法）

**视图**：
你此刻看这块板的方式：一个缩放 `s` + 一个平移 `t`。缩放平移不改数据一个字节。

**视图映射**：
"世界坐标 → 屏幕坐标"那一条公式（屏幕 = 世界 × s + t），以及它在三种落地上的一致写法
（DOM 浮层的位置、canvas 的变换、SVG 的变换）。**口径只允许有一份**：谁要 round、
dpr 怎么叠、捏合的锚点怎么跟，都在这儿定。
_避免_：坐标转换、transform（那是 canvas 的某个 API）

**补正**：
视图正在变的那些帧里，**两组东西的进度不一样**：墨迹 / 连线要等一次 canvas 重画，
卡片 / 板框要等一次 React 提交。补正就是这两者之间那份**差**，写成一条变换贴在
**所有跟着视野走的层**上（卡片层 + 两层 canvas + 两层 SVG），在笔势回调里同步写下，
等重画完成（差变成 0）就**当场撤掉**。所以它是**过渡量**：不长期挂着、松开手就没了。
数学是 `translate(live.t − drawn.t × k) scale(k)`、`k = live.s / drawn.s`，
`transform-origin` 必须是 `0 0`。
★ **基准是"已经画出来了的那一版"**（`drawn`），不是"已经提交但还没重渲染"的那一版 ——
一次拖动里同时存在三个视图（手指算出的 / 画出来的 / 提交了的），三者两两差一步，
用错基准差就翻倍（README 第 46 条记着这个 bug 的现场）。
_避免_：动画、缓动、插值（它不是在补一段好看的动作，是在补真实存在的先后差）、
"又加了一层 transform"（差为 0 时那个变换就是"没有变换"，见 README 第 46 条）

**世界坐标 / 屏幕坐标**：
前者是数据（笔迹的点、卡片的位置，存进文件的就是它），后者是像素（相对画布容器）。
只有一处能把两者换过来 —— 见「视图映射」。

### 把笔迹变成卡片

**框选**：
用框圈住一块东西（笔杆侧键或工具条上的框选）。它是"对这一块做点什么"的入口。
圈到的是**笔迹**（碰着就算）和**卡片**（中心落在框里才算）—— 后者和「留下板框」「复制」
是同一句话（`geometry.js` 的 `membersInBox`）。
⚠ **固定（📌）住的卡片框不进来** —— 那是它的全部意思："别动我"。

**选中**：
框住之后那一撮东西（笔迹 + 卡片，或者正好那一条连接）处在"可以动它"的状态：
删 / 挪 / **放大缩小** / **旋转** / 认成卡片 / 留下板框 / 改词。它是**界面状态**
（屏幕上那个虚框、四角那几颗手柄和那排按钮），不存盘。
_避免_：选择集、selection（那是"框住的这一撮"这一种的说法；"现在在动哪一样"见「焦点」，
规矩在 `selection.js`）

**焦点**：
"你现在在动哪一样东西" —— 只能有**一个**：一张卡片、一个板框、一撮框住的东西，或者没有。
它同时决定三件事：屏幕上谁亮着、**Delete 删谁**、**Esc 先收哪一层**。
也是界面状态，不存盘。规矩只在 `focus.js` 一处 —— 互斥是**结构**的
（一个记录只有一种 kind），不是二十处"选这个要清那个"的 if。
⚠ "框住的那一撮"（kind=`ink`）里能同时装**笔迹和卡片**（`ids` + `cards`）——
那不是"同时选中两样"，而是**一次框选的产物**（Delete 把两样一起删）。
⚠ 还有一条：**面板上的按键不是纸面上的按键**（Delete / Backspace 只在焦点位于画布上时才算）。
_避免_：选中集、active、current、currentTarget

**美化手写**：
把圈住的笔迹认成**文字**，用你挑的字体排成一张卡。原笔迹保留。

**手写公式 / 写字板**：
另开一块小板写公式，认成 LaTeX 落成公式卡。和"框选认公式"是两条路，同一套识别服务。

**规整形状**：
收笔之后**停顿半秒**，把这一笔**换成一个规整的几何形态** —— 你画个不太圆的圈，它变成真正的圆；
抖一点的直线变成笔直的线；还有矩形和三角形。规整的是**这一笔本身**（位置、`id` 不动，
所以撤销一步就回去、连接和选中都还在），压力一律 0.5（规整形态是几何产物，没有手压）。
判读那半的规矩在 `shapes.js`，"认出来之后怎么动"那半在 `shape-object.js`。
★ **它和「连接」是两件事**：规整不猜任何含义 —— 你画了个圆圈，它就是把圆圈画圆。
"猜你画这个形状是什么意思"那件事已经**删掉了**（ADR-0001、README 第 36 条）。
★ 铁律：**宁可认不出来，也不许认错** —— 认不出就原样留着，什么都不改。
所以汉字的一横一撇（弦长不到 110 世界像素）不许被认成直线。
★ **画大画小一视同仁**：判据里**不许有"按尺寸成比例的采样密度"**这种东西
（`点数 ÷ 对角线` 那种）—— 它的分子与尺寸无关、分母随尺寸线性长，
于是"画得越大越容易被判成认不出来"（用户 2026-09-19 报的正是这个，见 README 第 51 条）。
"点够不够多"由 `MIN_PTS`（绝对点数，和尺寸无关）单独负责。
★ 只有**最后收的那一笔**在等：下一笔一落下，上一个待办就作废。
所以写字时永远不会攒出一批待办 —— 误判的暴露面就是"你正在看的那一笔"。
★ 认错了 `Ctrl+Z` 一步退回，而且**退掉之后它不会再自己变回来**（不然撤销就是假的）。
_避免_：吸附、自动识别图形、形状纠正、识别（"识别"在这个项目里指手写→文字/公式）

**图形**：
一个**规整过的**形状（圆 / 椭圆 / 矩形 / 三角形 / 直线）。它不只是"一堆摆得很正的点"：
记在笔迹的 `shape` 字段上，存的是**几何参数**（圆心 + 两个半轴 + 角度），点是算出来的，
所以**重开一张板它还记得自己是图形**。
_避免_：形状对象、shape object（`shape` 是它的存储字段名）、图形卡（那是卡片，不是这个）
★ 缩放**会改变形状的身份**：横竖分开拉，圆变成椭圆、正方形变成长方形（那正是"修正真正形状"）。

**选区手柄**（四角 + 旋转，画在"框住的那一撮"四周）：
拖角 = 缩放（对角那一点不动），右边中点那颗圆的 = 旋转（绕选区包围盒的中心）。
★ 它是**选区**的属性，不是"图形的属性"：几笔字、一坨乱涂、框进来的卡片都算"一个东西"
（用户 2026-09-21：「框选中任意的字迹——卡片都应该能够放大，旋转，这点类似 onenote」）。
规矩只在 `selection.js` 的 `transformPick` 一处，界面那一半在 `Board.jsx` 的两段手势里。
★ **笔迹分轴、卡片等比**：笔迹是点，横着拉就是"字被拉宽"；卡片的内容是**文字**
（`w` 是硬约束，拉窄了当场裁内容），所以它只吃几何平均 `√(fx·fy)`，
而**位置**照样按完整的 fx/fy 走。
★ 旋转柄摆**右边中点**而不是顶边中点：顶边那一带被框选浮层那排按钮占着（实测两者正好压在一起）。
★ 一次拖动 = 一步撤销；每一帧都从"按下那一刻的板"**重算**（累加会攒误差，而且拖十下退十次）。
★ 三条决定（卡片等比、`rot` 存字段、转轴是选区中心）写在 **ADR-0003**；
"卡片的 `rot` 怎么读怎么写"在 `board.js`，"怎么动"在 `selection.js` 的 `transformPick`。

### 把这一块搬到别处

**剪贴板 / 复制粘贴**：
把**框住的那一块**（笔迹 + 框住的卡片 + 板框）打包带走，贴到**另一张板**上。
这件事的重点在"另一张板"：同一个板里复制粘贴没有真问题，跨板才有三件事要各自解决 ——
**只装内容不装关系**（连接的两端是别人的 id）、**落点用相对偏移**（同一张板上搬，原点不一样）、
**贴出来一律是新 id**。
_避免_：clipboard（那是浏览器 API 的名字 —— 我们**没用**它，用的是 localStorage）、
拷贝（"复制"就够了）
规矩只在 `clipboard.js` 一处（`copySelection` / `pastePayload`，纯函数）；
存哪儿是界面的事（`Board.jsx` 的 `CLIP_KEY`）。
★ `navigator.clipboard` 在 `http://127.0.0.1` 上写不进去（要 HTTPS + 用户手势），
所以别往那个方向改。

### 把笔记发出去

**导出**：
把一条笔记变成一个**离开这个程序也能看的网页**（`data/.导出/某一课.html`）。
它做的不是"复制文件"，是**把三样机器记号翻译成人人也看得懂的样子**：
`$公式$` 排出来、`[[引用]]` 变成能点的链接、缩进变成嵌套的块。
一次导出 = 那一刻的**快照**，之后改笔记它**不会自己更新**（重导一份就是新的）。
_避免_：保存、下载、打印（那说的是浏览器的事）

**导出目录（`.导出`）**：
导出落在**源笔记同一层**的 `.导出/` 里（`data/大物/电磁学/` 里的笔记 →
`data/大物/电磁学/.导出/`）。这样"这一课的导出"永远跟着这一课走。
名字前面那个点是有用的：服务端列目录时跳过 `.` 开头的，所以左栏**不会多出一个
点进去空空的目录**（那里面全是 `.html`，而这个应用只认 `.md`）。
用户找它走左栏底部那一行（明写着的入口），不靠它在树里露脸。

**名词表**：
导出文档底部那一张"量 → 说明 → 有几处用到它"。量那一支在原文里是一节，
但它读起来像字典，所以收成一张表 —— 正文干净、查词方便，
而且**点引用的名字跳过去一定有东西**。

**摘括号**：
导出文档里 `[[B]]` 显示成 **`B`**（把方括号摘掉），靠蓝色+下划线表示"这是个可点的词"。
**界面上不摘**（那里显示 `[[B]]`）—— 因为使用者知道这个记号，而且那是个按钮。
但导出是发给**没读过说明书的人**的，一对中括号对他来说就是乱码，
那正是导出要消掉的三样机器记号之一。
_例外_：**断引用不摘** —— 要让人看出"这里本来该有个链接"，
留个记号比一个光秃秃的词更诚实。

**自包含**：
导出文件的每一样东西（公式字体、样式）都**装在文件自己里面**，断网也长一个样。
代价是文件大一点（公式字体嵌进去约 1 MB），但对方可能在地铁上、
可能公司网络拦了字体站 —— 那是"点开就能看"的价钱。

### 会混的词（说清楚）

**固定（📌）** 说的是**卡片**：钉在纸上，拖不动、选不中，变成纸的一部分。存成 `locked`。
_避免_：锁定、钉住、pinned —— **`pinned` 已经被「视角定住」占了**，两个完全不是一回事。

**视角定住** 说的是**视图**：这条视野是你亲手拖/缩定的，下次打开别自动适配。存成 `viewPinned`。

### 自检用的词（也说清楚）

**夹具板**：
自检给自己造的一张板（`data/board-zz-<标签>.md`）。它归造它的那条自检所有 —— 跑完就删，
而且**名字不是 `board-zz-*` 的一律不删**（"删的是谁的"这个坑的答案就是这个前缀）。
应用通过 `?file=<夹具名>` 直接从 URL 打开它，于是自检**根本不会读到用户那张板**。
_避免_：测试数据、临时板、fixture

**用户数据守卫**：
真浏览器自检跑前对 `data/` 里**原有**文件拍一份快照（内容哈希 + 原文 + mtime），跑完发现谁变了
就报红一条。判据是**内容哈希**不是 mtime —— 应用经常"写回一样的内容"。
它把"自检不许碰用户的数据"从一句承诺变成了一道结构。
★ 快照是**递归**的：`data/` 能分层之后，"只读顶层"等于"用户的板挪进子目录就不保护了"。
★ 快照里**不收自检自己的夹具**（`board-zz-*`）：那些是自检造、自检删的，
  算进去就必然报一条"自检动了你的文件（不见了）"的**假红**（2026-09-18 踩的，见第 48 条）。
★ **守卫不写回任何用户文件**（2026-09-18 定的，见 README 第⑩坑）。
原来它按快照恢复 —— 那会吃掉正在编辑的人刚敲的字。
现在只报 ✗ + 把"被改动之后的那一版"留证到 `.cache/lost-found/`。
★ 它还要**分清是谁改的**：跑之前就是热的（有程序在写）→ 不是自检的锅，只提示不报错。
_避免_：备份、回滚
