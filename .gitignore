node_modules/
dist/
.cache/
data/*.tmp-*

# 手写识别的密钥放在这里，绝不进 Git。
# 见 README「手写公式」那一节：密钥只存在你自己的电脑上，页面读不到它，
# 只有本地服务在代理请求时才用它。换电脑要重新填一次。
config/

# agent（WorkBuddy）的工作日志：.workbuddy/memory/*.md
# 它记的是"这台机器上发生了什么"——本机路径、环境备忘、登录过程之类。
# 仓库是**公开**的（见 README「备份到 GitHub」），这些没必要发出去；
# 文件本身还在本机，只是不进 Git。真要留档就自己挑有用的抄进 docs/。
.workbuddy/
